services:
  - type: web
    name: todo-app
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn word_app:app
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
